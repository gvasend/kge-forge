# Governed Codex Host (A2)

This control-plane package exposes only governed operations to an agent-facing
adapter. It is deliberately independent of Fabrik and `kge-forge-demo`.

Run the scratch qualification with `python3 -m unittest discover -s adapter/tests`.
